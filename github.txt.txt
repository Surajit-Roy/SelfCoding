----CREATE REPOSITORY----
touch .gitignore
git config --global user.name Surajit65
git config --global user.email "surajit9064@gmail.com"
git init
git add .
git commit -m "Initial Commit"
git status # Just to see
ssh-keygen -t rsa -b 4096 -C "surajitroy9064@gmail.com"
tail <path to id_rsa.pub> # Add this key to your GitHub Account
git remote add origin <ssh url.git>
git push origin master

----UPDATE REPOSITORY----
git add .
git commit -m "Added github Cheetsheet"
git push origin master